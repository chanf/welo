# Welo 后端开发总结

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目 | Welo 多团队任务管理系统 |
| 开发阶段 | V1 MVP 后端第一阶段 |
| 开发日期 | 2026-09-07 |
| 运行平台 | Cloudflare Workers |
| 数据库 | Cloudflare D1 |
| API 前缀 | `/api/v1` |
| 后端入口 | `src/index.ts` |

## 2. 本次开发目标

本次开发从空仓库开始搭建后端基础能力，目标是让前端能够开始进行登录、团队、小组、项目、任务、甘特图和工作台页面的真实接口接入。

后端实现遵循现有产品设计、后端详细设计和数据库设计文档，重点落地以下原则：

1. Worker 无状态运行，不使用内存会话或本地文件存储；
2. 使用 D1 保存用户、组织、项目、任务和会话数据；
3. 所有受保护接口由服务端根据会话重新读取用户角色和组织关系；
4. 普通成员只能看到自己所属团队和小组内的数据；
5. 项目和任务使用软删除；
6. 甘特图排期更新使用 `updatedAt` 乐观并发控制；
7. 密码和会话令牌不以明文保存。

## 3. 已完成内容

### 3.1 项目基础设施

- 新增 `package.json`，配置 Hono、Zod、Wrangler、TypeScript；
- 新增 `tsconfig.json`；
- 新增 `wrangler.jsonc`；
- 新增 `.gitignore`；
- 配置 Cloudflare Workers module worker 入口；
- 配置 D1 数据库 binding：`env.DB`；
- 配置 `ENVIRONMENT`、`SESSION_TTL_DAYS`、`CORS_ORIGINS`；
- 配置每 30 分钟执行一次的 Cron Trigger；
- 开启 Workers Observability；
- 开启 `nodejs_compat`，方便 Workers 依赖运行。

### 3.2 数据库

新增 `migrations/0001_initial.sql`，包含以下表：

| 表 | 用途 |
| --- | --- |
| `users` | 用户、角色、登录失败次数和锁定状态 |
| `teams` | 团队 |
| `team_members` | 用户与团队关系 |
| `team_groups` | 团队小组 |
| `group_members` | 用户与小组关系 |
| `projects` | 团队项目 |
| `tasks` | 项目任务 |
| `sessions` | 登录会话 |

同时增加团队成员、用户小组、任务项目小组、任务负责人和会话过期时间等查询所需索引。

任务表使用数据库约束保证：

- 开始日期不能晚于结束日期；
- `done` 状态必须有 `completed_at`；
- 非 `done` 状态不能有 `completed_at`；
- 任务标题、详情长度符合设计要求；
- 任务状态和优先级只能使用约定枚举。

### 3.3 认证和安全

已实现：

- 用户注册；
- 邮箱统一转小写；
- 用户名和邮箱唯一性检查；
- PBKDF2-SHA256 密码哈希；
- 登录成功后创建会话；
- HttpOnly Cookie 会话；
- Bearer Token 会话；
- 当前会话查询；
- 退出当前会话；
- 连续登录失败 5 次后锁定 15 分钟；
- 过期会话自动拒绝；
- Cron 清理过期会话；
- 响应中不返回密码哈希、失败次数、锁定字段和 token hash；
- CORS 使用配置白名单，不允许任意来源携带凭证。

Cookie 名称：

```text
welo_session
```

### 3.4 组织管理

已实现：

- 查询当前用户可访问团队；
- 超级管理员创建团队；
- 查询团队详情；
- 超级管理员更新或归档团队；
- 超级管理员查询团队成员；
- 超级管理员添加团队成员；
- 超级管理员移除团队成员；
- 查询团队小组；
- 超级管理员创建小组；
- 超级管理员更新或停用小组；
- 超级管理员添加小组成员；
- 超级管理员移除小组成员。

关键业务约束：

- 普通成员不能管理团队、小组和成员；
- 停用团队为只读；
- 归档团队为只读；
- 小组成员必须先属于对应团队；
- 移除团队成员前，如果仍负责未删除任务，则返回冲突错误；
- 移除团队成员时同步移除其在该团队下的小组关系。

### 3.5 项目管理

已实现：

- 分页查询团队项目；
- 按关键词筛选项目；
- 按 `active` / `archived` 筛选项目；
- 创建项目；
- 查询项目详情；
- 超级管理员更新项目；
- 超级管理员归档项目；
- 超级管理员按项目名称确认软删除项目；
- 返回项目任务数量。

权限规则：

- 普通成员只能访问自己所属团队的项目；
- 普通成员可以在所属团队创建项目；
- 归档项目只读；
- 删除项目不会立即物理删除，而是写入 `deleted_at`。

### 3.6 任务管理

已实现：

- 项目内任务分页查询；
- 按小组、负责人、状态、优先级筛选；
- 按关键词筛选标题和详情；
- 按结束日期、创建时间、优先级排序；
- 创建任务；
- 查询任务详情；
- 修改任务；
- 超级管理员软删除任务；
- 状态切换；
- 负责人和小组变更校验；
- 任务完成时间自动维护。

权限规则：

- 超级管理员可以访问当前团队项目下全部任务；
- 普通成员必须属于项目团队；
- 普通成员查看任务时，必须属于任务小组；
- 普通成员可以修改自己可见的任务；
- 普通成员不能删除任务；
- 任务负责人必须属于任务小组；
- 停用小组不能创建新任务；
- 归档项目和归档团队不能写入任务。

### 3.7 甘特图和工作台

已实现：

- 查询项目甘特图数据；
- 返回甘特图日期范围；
- 无开始日期的任务提供虚拟开始日期；
- 甘特图范围前后扩展 3 天；
- 拖拽保存任务排期；
- 使用 `expectedUpdatedAt` 防止覆盖其他请求的更新；
- 返回 `VERSION_CONFLICT` 让前端重新加载；
- 团队工作台统计；
- 当前进行中项目数量；
- 当前用户可见任务数量；
- 当前用户未完成任务数量；
- 逾期任务数量；
- 未来 7 天截止任务数量；
- 最近项目；
- 即将截止任务。

### 3.8 超级管理员后台

已实现：

- 平台用户数量；
- 活跃团队数量；
- 进行中项目数量；
- 未删除任务数量；
- 用户分页查询；
- 按关键词查询用户；
- 按系统角色筛选用户；
- 授予或撤销超级管理员角色；
- 防止当前超级管理员撤销自己的管理员权限。

## 4. 统一响应格式

成功响应：

```json
{
  "data": {},
  "meta": {
    "requestId": "req_xxx",
    "pagination": null
  }
}
```

分页响应：

```json
{
  "data": [],
  "meta": {
    "requestId": "req_xxx",
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 42,
      "totalPages": 3
    }
  }
}
```

错误响应：

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求参数校验失败",
    "details": [
      {
        "field": "endDate",
        "reason": "结束日期不能为空"
      }
    ]
  },
  "meta": {
    "requestId": "req_xxx"
  }
}
```

## 5. 当前错误码

| HTTP 状态码 | 错误码 | 说明 |
| --- | --- | --- |
| 400 | `VALIDATION_ERROR` | 请求参数不合法 |
| 401 | `UNAUTHENTICATED` | 未登录或会话失效 |
| 401 | `INVALID_CREDENTIALS` | 账号或密码错误 |
| 403 | `FORBIDDEN` | 已登录但无权操作 |
| 404 | `NOT_FOUND` | 资源不存在或当前用户不可见 |
| 409 | `CONFLICT` | 唯一键、状态或业务规则冲突 |
| 409 | `MEMBER_HAS_ASSIGNMENTS` | 成员仍负责任务 |
| 409 | `VERSION_CONFLICT` | 数据已被其他请求更新 |
| 423 | `ACCOUNT_LOCKED` | 账号暂时锁定 |
| 500 | `INTERNAL_ERROR` | 未预期服务端错误 |

## 6. 验证情况

已通过：

```text
npm install
npm run build
git diff --check
wrangler deploy --dry-run
```

其中 `npm run build` 使用 TypeScript 编译检查，当前通过。

`wrangler deploy --dry-run` 能够完成 Worker 打包并识别 D1、环境变量等 binding。当前沙箱环境不允许 Wrangler 写入用户目录日志，也不允许监听 `127.0.0.1`，因此本地 D1 migration 和 `wrangler dev` 的实际启动验证受环境限制。

## 7. 当前遗留项

以下内容暂未实现，前端暂不应接入：

- 登录和写接口限流；
- Cookie 认证非 GET 请求的 CSRF Token；
- 密码找回和修改密码；
- 任务操作历史；
- 评论；
- 附件；
- 任务依赖；
- 通知；
- 成员视图；
- 团队成员小组详情的完整 DTO；
- 完整的自动化测试和端到端测试；
- 远程 Cloudflare D1 migration；
- staging / production 的真实数据库 ID 和域名配置。

## 8. 后续建议

建议后续按以下顺序推进：

1. 先让前端接入认证、团队、项目和任务主流程；
2. 补本地 D1 fixture 和 API 集成测试；
3. 补完整的成员、小组和 DTO 映射；
4. 增加限流、CSRF 和生产 CORS 配置；
5. 创建 staging D1 并执行 migration；
6. 完成前后端联调后，再进行 Cloudflare 部署。

